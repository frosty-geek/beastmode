/**
 * Step definitions for wave-failure integration tests.
 *
 * Extends the watch-loop steps with scenario-specific controls for multi-wave
 * feature ordering and dispatch.
 */

import { When } from "@cucumber/cucumber";
import type { WatchLoopWorld } from "../support/watch-world.js";

// Note: Most steps are reused from watch-loop.steps.ts.
// This file exists for future extension when adding failure scenarios.

// Placeholder for future failure-specific steps
// (Currently all steps are handled by watch-loop.steps.ts)
