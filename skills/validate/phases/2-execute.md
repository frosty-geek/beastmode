# 2. Execute

## 1. Run Tests

Run project test command. Capture output and exit code.

## 2. Run Lint (if configured)

Run project lint command if configured.

## 3. Run Type Check (if configured)

Run project type check command if configured.

## 4. Run Custom Gates

Execute any custom gates defined in `.beastmode/meta/VALIDATE.md`.
