---
phase: release
slug: handoff-v2
bump: patch
status: completed
---

# Release: handoff-v2

**Bump:** patch
**Date:** 2026-03-29

## Highlights

Normalizes all five beastmode checkpoint phases to follow a uniform contract: standardized frontmatter (`phase`, `slug`, `status`), consistent STOP termination, and unified `beastmode <phase> <slug>` handoff format. Fixes worktree output pre-reconciliation in the scanner.

## Fixes

- Pre-reconcile worktree outputs in scanner, preserve manifests after release

## Chores

- Normalize design checkpoint frontmatter to include `status` field
- Normalize plan checkpoint frontmatter with `slug`, `status`, `epic`, `feature`
- Normalize implement checkpoint to always write completion artifact and use `beastmode validate <slug>` handoff
- Normalize validate FAIL path STOP text to `STOP. No additional output.`
- Normalize release checkpoint to include `status` in frontmatter and end with STOP

## Full Changelog

- `013a124` validate(handoff-v2): checkpoint
- `6fba59f` implement(plan-frontmatter): checkpoint
- `a48feea` implement(implement-normalize): checkpoint
- `d9bd7ad` implement(validate-stop): checkpoint
- `e1f9608` plan(handoff-v2): checkpoint
- `31dcff5` design(handoff-v2): checkpoint
- `1922351` fix: pre-reconcile worktree outputs in scanner, preserve manifests after release
