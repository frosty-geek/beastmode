# .beastmode/config.yaml

cli:
  dispatch-strategy: iterm2            # sdk | cmux | auto

github:
  enabled: true                     # Set to true by /beastmode setup-github
  project-name: "Beastmode Pipeline" # Projects V2 board name
