# Release v0.4.0

**Date:** 2026-03-04

## Highlights

Progressive L1 docs — fractal knowledge hierarchy that fixes the broken meta layer and restructures all UPPERCASE files as progressive enhancements.

## Features

- New `.beastmode/CLAUDE.md` manifest — pure @imports hub wiring all L0/L1 files into sessions
- Root `CLAUDE.md` simplified to single import + Prime Directives
- `PRODUCT.md` enriched as L0 — richest standalone project summary
- All 15 L1 files (context, meta, state) rewritten with progressive format: summary + section summaries + @imports
- All 6 L2 detail files gain "Related Decisions" sections linking to L3 state artifacts
- Retro bottom-up bubble — 3-checkpoint propagates summaries L2 → L1 → L0
- Retro-context agent gains hierarchy awareness for drift detection

## Fixes

- Meta learnings now loaded into sessions (previously missing from import chain)
- State L1 files now loaded into sessions (previously missing from import chain)

## Full Changelog

- feat(progressive-l1-docs): fractal knowledge hierarchy with .beastmode/CLAUDE.md manifest
