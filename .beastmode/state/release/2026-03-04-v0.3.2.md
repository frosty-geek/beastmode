# Release v0.3.2

**Date:** 2026-03-04

## Highlights

Consolidates all project artifacts under `.beastmode/` by eliminating the `.agents/` directory. Tracked artifacts (design, plan, research) move to `.beastmode/state/`, session state (status, tasks) moves to `.beastmode/sessions/`.

## Chores

- Migrate 7 tracked artifacts from `.agents/` to `.beastmode/state/{design,plan,research}/`
- Update 12 skill phase files to reference new `.beastmode/` paths
- Update 4 context docs, 2 agent docs, README, and META with new locations
- Replace `.agents/worktrees/` gitignore with `.beastmode/sessions/`
- Delete `.agents/` directory entirely

## Full Changelog

Refactoring: `.agents/` directory removed. All paths consolidated under `.beastmode/`.
