# Validate Context

Quality gates and verification strategy before release. Currently relies on manual verification via skill invocation and content inspection. The /validate skill runs project-specific checks (tests, lint, type checks) and requires user approval before proceeding to release.

No L2 detail files yet — quality gate definitions will be added as formal gates emerge beyond manual verification.
