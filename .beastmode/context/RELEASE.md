# Release Context

Release workflow and versioning conventions. The /release skill handles version detection from plugin.json, commit categorization for changelog generation, plugin marketplace updates, worktree merge and cleanup, and retro capture before commit.

No L2 detail files yet — versioning strategy and changelog format docs will be added as the release process matures.
