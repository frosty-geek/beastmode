# Manifest Lifecycle

## Context
The manifest-file-management design splits manifest logic into two modules and renames directories: `pipeline/` becomes `state/` (gitignored manifests), `state/` becomes `artifacts/` (committed skill outputs).

## Decision
CLI creates manifest at first phase dispatch (design) via store.create(slug) before dispatching — manifest exists throughout the entire skill session. Enriched from output.json (auto-generated by Stop hook from artifact frontmatter) at each checkpoint. CLI is the sole mutator via two modules: manifest-store.ts (get, list, save, create, validate, rename, find, slugify — sole fs accessor) and manifest.ts (enrich, advancePhase, regressPhase, markFeature, cancel, deriveNextAction, checkBlocked, shouldAdvance — pure functions returning new manifests). Manifest location: `.beastmode/state/YYYY-MM-DD-<slug>.manifest.json` (flat-file convention, local-only, gitignored). CLI rebuilds from worktree branch scanning on cold start. Terminology: `slug` is an immutable 6-character hex assigned at worktree creation; `epic` is the human-readable name derived by the design skill. `store.rename()` atomically renames all slug-keyed resources (artifacts, branch, worktree, manifest file, manifest content) with prepare-then-execute strategy. `store.find()` resolves by either hex or name. `originId` field tracks lineage (birth hex to final epic name). Single `store.save()` per dispatch — machine persist action accumulates state in memory only.

## Rationale
Two-module split separates filesystem concerns from business logic, making state machine functions testable with plain objects and no mocks. store.create() before dispatch ensures the manifest exists for the entire session, so post-dispatch always has a manifest to enrich. Directory rename makes names match contents: `artifacts/` for committed skill outputs, `state/` for gitignored pipeline state.

## Source
.beastmode/artifacts/design/2026-03-29-github-cli-migration.md
.beastmode/artifacts/design/2026-03-29-manifest-file-management.md
.beastmode/artifacts/design/2026-04-01-slug-redesign.md
